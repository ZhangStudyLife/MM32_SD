# MM32_SD 项目日志

用于记录本仓库的重要更改、问题修复与待办事项。建议按日期倒序追加，简洁明确、可检索。

## 2025-09-22

- 录制写盘前先将帧快照到 `image_copy` 再写入 SD，显著降低“断层/混帧”。文件：`32_SD/USER/src/main.c`
- 去除录制分支中的 `continue`，确保本轮循环调用 `display()` 扫描按键，KEY2 可正常停止录制。文件：`32_SD/USER/src/main.c`
- 录制期间不再显示相机图像，开始录制时清屏以移除旧图像。文件：`32_SD/USER/src/main.c`
- 保存前若检测到新帧，先快照再存 BMP，避免单帧断层。文件：`32_SD/USER/src/main.c`
- 交互式菜单与参数记忆，支持倍速校准；修复 Windows 播放兼容性。文件：py脚本
